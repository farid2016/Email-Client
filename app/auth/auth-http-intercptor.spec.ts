import { AuthHttpIntercptor } from './auth-http-intercptor';

describe('AuthHttpIntercptor', () => {
  it('should create an instance', () => {
    expect(new AuthHttpIntercptor()).toBeTruthy();
  });
});
